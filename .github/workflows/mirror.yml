name: 'mirror'
on:
  push:
    branches:
        - __mirror
  schedule:
    # Run every 3 min
    - cron: '*/3 * * * *'
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    name: mirror
    steps:
      - name: mirror
        id: mirror
        uses: bridgelightcloud/github-mirror-action@v2
        with:
          origin: 'https://github.com/n14s/blink-break.git'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
